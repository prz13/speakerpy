from enum import Enum


class SettingGame(Enum):
    baldur_gate_3 = dict(
        bottom_percentage=40,
        width_percentage=19,
        keep_color_hex="#ffffff",
        keep_tolerance=90,
    )
